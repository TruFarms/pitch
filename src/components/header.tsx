// components/header.tsx

export function Header() {
  return (
    <header className="py-4 px-4 sm:px-6 md:px-8 h-24 sm:h-36">
      {/* Header is now empty, but kept for layout spacing */}
    </header>
  );
}
